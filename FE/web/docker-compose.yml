version: '3.2'
services:
  sample:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5174"
    container_name: front
    restart: always
    image: ssafy10b108/fe:ver.1.0.0